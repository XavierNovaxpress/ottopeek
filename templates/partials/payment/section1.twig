<section class="my-5">
    <div class="container">
        <div class="row  align-items-center">
            <div class="col-md-6">
                <h2 class="text-primary">{{ (channel ~ '.payment.section1.titre')|trans|raw }}</h2>
                <h3 class="text-dark">{{ (channel ~ '.payment.section1.sous-titre')|trans|raw }}</h3>
                <p>{{ (channel ~ '.payment.section1.description')|trans|raw({'%companyName%': companyName}) }}</p>
            </div>
            <div class="col-md-6">
                <img src="https://place-hold.it/460x570" alt="{{ (channel ~ '.home.section1.image.alt')|trans|raw }}" class="img-fluid rounded lazyload">
            </div>
        </div>
    </div>
</section>
