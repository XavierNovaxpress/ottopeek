<section class="container my-5">
    <div class="row">
        <div class="col-md-6">
            <img src="{{ webrootURL }}/public/media/{{ channel }}/image7.png" alt="{{ (channel ~ '.home.section3.image.alt')|trans|raw }}" class="img-fluid rounded">
        </div>
        <div class="col-md-6">
            <h2 class="text-primary">{{ (channel ~ '.home.section3.titre')|trans|raw }}</h2>
            <p class="lead">{{ (channel ~ '.home.section3.description')|trans|raw }}</p>
            <ul class="list-group">
                <li class="list-group-item">
                    <strong>{{ (channel ~ '.home.section3.item1.titre')|trans|raw }}</strong>
                    <p>{{ (channel ~ '.home.section3.item1.description')|trans|raw }}</p>
                </li>
                <li class="list-group-item">
                    <strong>{{ (channel ~ '.home.section3.item2.titre')|trans|raw }}</strong>
                    <p>{{ (channel ~ '.home.section3.item2.description')|trans|raw }}</p>
                </li>
                <li class="list-group-item">
                    <strong>{{ (channel ~ '.home.section3.item3.titre')|trans|raw }}</strong>
                    <p>{{ (channel ~ '.home.section3.item3.description')|trans|raw }}</p>
                </li>
                <li class="list-group-item">
                    <strong>{{ (channel ~ '.home.section3.item4.titre')|trans|raw }}</strong>
                    <p>{{ (channel ~ '.home.section3.item4.description')|trans|raw }}</p>
                </li>
            </ul>
        </div>
    </div>
</section>
