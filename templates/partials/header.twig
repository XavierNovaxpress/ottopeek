<header class="bg-white border-bottom">
    {% include 'partials/_header/' ~ channel ~  '/alert.twig' %}
    <div class="container d-flex justify-content-between align-items-center pb-2">
        {% include 'partials/_header/' ~ channel ~  '/menu.twig' %}
        <div class="row align-items-center">
          <div class="d-none d-lg-block col auto">
              {% include 'partials/_header/' ~ channel ~  '/connexion.twig' %}
          </div>
            <div class="d-none d-lg-block col auto">
              {% include 'partials/_header/' ~ channel ~  '/language-selector.twig' %}
          </div>
        </div>
    </div>
</header>
