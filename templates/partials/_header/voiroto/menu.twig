<nav class="navbar navbar-expand-lg navbar-light  col auto p-0">
    <a class="navbar-brand" href="/home?lang={{locale}}&channel={{ channel }}">
        <img src="{{ webrootURL }}/public/media/{{ channel }}/logo.svg" alt="{{ (channel ~ '.header.logo')|trans|raw }}" class="img-fluid">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item mx-4">
                <a class="nav-link text-dark" href="/pricing?lang={{locale}}&channel={{ channel }}">{{ (channel ~ '.header.prix')|trans|raw }}</a>
            </li>
            <li class="nav-item mx-4">
                <a class="nav-link text-dark" href="#">{{ (channel ~ '.header.aide')|trans|raw }}</a>
            </li>
            <li class="nav-item mx-4">
                <a class="nav-link text-dark" href="#">{{ (channel ~ '.header.actualites')|trans|raw }}</a>
            </li>
            <li class="nav-item mx-4">
                <a class="nav-link text-dark" href="#">{{ (channel ~ '.header.contact')|trans|raw }}</a>
            </li>
        </ul>

        <div class="d-block d-lg-none">
          {% include 'partials/_header/' ~ channel ~  '/connexion.twig' %}
          {% include 'partials/_header/' ~ channel ~  '/language-selector.twig' %}
        </div>

    </div>
</nav>
